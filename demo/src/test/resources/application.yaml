spring:
  application:
    name: Demo
  datasource:
    url: jdbc:postgresql://localhost:5433/demo
    username: user
    password: 1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
